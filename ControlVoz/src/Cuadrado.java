import java.awt.*;

public class Cuadrado extends Rectangulo{
    public Cuadrado(int size, int x, int y, Color color, int zIndex, String identificador) {
        super(size, size, x, y, color, zIndex, identificador);
    }
}
